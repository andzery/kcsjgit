/*¸øÈô¸É¸ö×Ö·û´®ÅÅÐò   */
#include "stdio.h"
#include "string.h"
#define M 5
#define N 10
void main()
{char word[M][N],t[N];
 int i,j;
 for(i=0;i<M;i++)
  gets(word[i]);
 for(i=1;i<M;i++)
  for(j=0;j<M-i;j++)
   if(strcmp(word[j],word[j+1])>0)
    {strcpy(t,word[j]);
     strcpy(word[j],word[j+1]);
     strcpy(word[j+1],t);
      }
  for(i=0;i<M;i++)
   printf("%s\n",word[i]);
   }
